<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.primolab.miner"
    android:versionCode="2"
    android:versionName="1.0.7">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Ask the OEM to stop reaping the mining service in the background. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Notification runtime permission (Android 13+, API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      extractNativeLibs="true" is REQUIRED: the miner ships as lib/arm64-v8a/libprimo.so
      and must be extracted to nativeLibraryDir, the one app dir Android mounts exec.
      We then exec it as a subprocess (see MinerService).
    -->
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:extractNativeLibs="true"
        android:allowBackup="false"
        android:theme="@style/PrimoDark">

        <activity
            android:name=".MiningActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ConfigActivity"
            android:exported="false"
            android:label="@string/config_title" />

        <activity
            android:name=".LogActivity"
            android:exported="false"
            android:label="@string/logs_title" />

        <service
            android:name=".MinerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
